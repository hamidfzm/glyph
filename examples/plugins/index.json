{
  "$comment": "Seed for the Glyph plugin marketplace. This file lives at glyph-md/plugins/index.json; the app reads it from REGISTRY_URL. Third-party entries point mainUrl at the plugin's own repo (a tagged release raw URL); bump version + mainUrl per release.",
  "plugins": [
    {
      "id": "com.glyph.hello-status",
      "name": "Hello Status",
      "description": "Sample plugin: a status bar greeting and a command palette entry.",
      "version": "1.0.0",
      "apiVersion": "^1.0.0",
      "mainUrl": "https://raw.githubusercontent.com/glyph-md/plugins/main/com.glyph.hello-status/main.js"
    }
  ]
}
