{
  "$schema": "https://raw.githubusercontent.com/nicedoc/tauri/v2/crates/tauri-utils/schema/capability.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities for Glyph. Android and iOS pickers hand back sandboxed content:// URIs that the Rust fs commands cannot open, so the fs plugin reads those instead (src/lib/documentContent.ts). Desktop is deliberately excluded: there the grant registry is the only filesystem gate.",
  "platforms": ["android", "iOS"],
  "windows": ["main", "w*"],
  "permissions": ["fs:default", "fs:allow-read-text-file"]
}
