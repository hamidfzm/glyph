# README media

Add these files next to this README so the images in the root [`README.md`](../../README.md) render on GitHub.

| File       | Purpose        | Suggested notes                          |
| ---------- | -------------- | ---------------------------------------- |
| `hero.png` | Main window    | ~1200–1600px wide; show TOC + content    |
| `demo.gif` | Short motion   | A few seconds; e.g. Mermaid or live reload |

**Recording (macOS):** Cmd+Shift+5 for screen recording; export GIF with [Kap](https://getkap.co/) or similar.

Keep file sizes modest so the repo and README stay fast (compress PNGs; trim GIF length and resolution).
