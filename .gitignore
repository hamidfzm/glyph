node_modules/
dist/
coverage/
src-tauri/target/
coverage/
junit*.xml
*.log
.DS_Store

# Claude Code local settings (keep .claude/ committed, ignore local overrides)
.claude/settings.local.json

# Platform-specific Tauri ACL schemas (regenerated locally per-host by
# `tauri dev` / `tauri build`, including the android/mobile ones from
# `tauri android dev/build`; only the cross-platform `desktop-schema.json`
# and `macOS-schema.json` are tracked upstream).
src-tauri/gen/schemas/linux-schema.json
src-tauri/gen/schemas/windows-schema.json
src-tauri/gen/schemas/android-schema.json
src-tauri/gen/schemas/mobile-schema.json
